import {loadMainPage, openVideo} from './media';
import {searchVideo} from './search';
import {filterVideo, setFilters} from './filter';

export const appActions = {
    loadMainPage,
    openVideo,
    searchVideo,
    filterVideo,
    setFilters
};

